import 'package:flutter/material.dart';

class Accesorios extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: AppBar(
        title: new Text("ACCESORIOS"),
      ),
      body: Center(
        child: Text("CATEGORIA DE ACCESORIOS"),
      ),
    );
  }
}